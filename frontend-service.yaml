apiVersion: v1
kind: Service
metadata:
  name: express-frontend-service
spec:
  type: NodePort
  selector:
    app: express-frontend
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000